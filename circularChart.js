var options = {
    series: [78],
    chart: {
      height: 120,
      type: 'radialBar',
    },
    plotOptions: {
      radialBar: {
        hollow: {
          size: '20%',
        }
      },
    }, 
    tooltip: {
        enabled: false, // Disable the tooltip to hide the series value
      },
    labels: [''],
  };

  var chart = new ApexCharts(document.querySelector("#circularChart"), options);
  chart.render();var options = {
    series: [78],
    chart: {
      height: 120,
      type: 'radialBar',
    },
    plotOptions: {
      radialBar: {
        hollow: {
          size: '20%',
        }
      },
    }, 
    tooltip: {
        enabled: false, // Disable the tooltip to hide the series value
      },
    labels: [''],
    
  };

  var chart = new ApexCharts(document.querySelector("#circularChart"), options);
  chart.render();
// second
  var options = {
    series: [95],
    chart: {
      height: 120,
      type: 'radialBar',
    },
    plotOptions: {
      radialBar: {
        hollow: {
          size: '20%',
        }
      },
    }, 
    tooltip: {
        enabled: false, // Disable the tooltip to hide the series value
      },
    labels: [''],
  };

  var chart = new ApexCharts(document.querySelector("#circularChart"), options);
  chart.render();

  